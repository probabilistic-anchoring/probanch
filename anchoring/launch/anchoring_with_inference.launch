<launch>

  <!-- Inference system -->
  <node name="dc_inference_node" pkg="inference" type="dc_inference"
        output="screen" />

  <!-- Anchoring framework -->
  <node name="anchor_management_node" pkg="anchoring" type="anchoring"
        output="screen" />

</launch>